# 🔬 Анализ исследования DeepSeek-OCR: Критические находки

**Дата:** 31.10.2025, 17:15  
**Статус:** 🔥 GAME CHANGER - Обнаружены неиспользованные возможности!

---

## 🚨 КРИТИЧЕСКИЕ НАХОДКИ

### 1. Parse Figure - СПЕЦИАЛЬНЫЙ РЕЖИМ ДЛЯ ДИАГРАММ! ⭐⭐⭐

**Цитата из исследования:**
> "Parse Figure (разбор схем/диаграмм) – структурированное извлечение данных из графиков, диаграмм и схем. В этом режиме модель пытается распознать не только текстовые надписи, но и **взаимосвязи между элементами диаграммы** (например, линии, стрелки, подписи к ним). **Этот режим как раз предназначен для понимания таких схем, как BPMN.**"

**ЧТО МЫ ДЕЛАЛИ НЕПРАВИЛЬНО:**
- ❌ Использовали `parse_figure` без дополнительных параметров
- ❌ НЕ указывали явно задачу извлечения связей
- ❌ НЕ использовали высокие разрешения (Large mode)
- ❌ НЕ активировали `crop_mode`

**ЧТО НУЖНО СДЕЛАТЬ:**
```python
# ПРАВИЛЬНЫЙ ВЫЗОВ Parse Figure для BPMN:
response = requests.post(
    "http://localhost:8000/ocr/figure",
    files={"file": img_data},
    data={
        "prompt_type": "parse_figure",
        "base_size": 1280,        # 🔥 Large mode!
        "image_size": 1280,       # 🔥 Высокое разрешение!
        "crop_mode": True,        # 🔥 Автообрезка!
        "custom_prompt": "Parse the BPMN diagram: list all elements (tasks, events, gateways) and their connections (arrows, flows). Provide element names and relationships in Russian."
    }
)
```

---

### 2. Режим Large (1280x1280) - ДЛЯ СЛОЖНЫХ СХЕМ! 🔥

**Цитата:**
> "Рекомендуется использовать высокое значение для схем: например, режим Large (base_size = 1280), позволяющий обрабатывать изображения до 1280×1280 пикселей для захвата мелких деталей. Повышение разрешения увеличивает количество vision tokens (визуальных токенов) до 400, что улучшает точность на «комплексных макетах, мелких деталях»."

**ЧТО МЫ ДЕЛАЛИ:**
- ❌ Использовали 1024x1024 (по умолчанию)
- ❌ НЕ тестировали Large mode (1280x1280)

**РАЗНИЦА:**
| Режим | base_size | Vision Tokens | Применение |
|-------|-----------|---------------|------------|
| Base | 1024 | 256 | Простой текст |
| **Large** | **1280** | **400** | **Сложные схемы с мелким текстом** |

---

### 3. Gundam режим - ИТЕРАТИВНОЕ СКАНИРОВАНИЕ! 🚀

**Цитата:**
> "В режиме 'Gundam' (особый пресет) используется crop_mode=True при base_size=1024, image_size=640 для итеративного сканирования сегментов изображения. В контексте BPMN-схем, активация crop_mode может повысить точность распознавания подписей к элементам, разбивая сложную диаграмму на более мелкие части для анализа."

**НОВАЯ СТРАТЕГИЯ:**
```python
# Gundam режим для BPMN:
data = {
    "base_size": 1024,
    "image_size": 640,     # 🔥 Меньше image_size для фрагментации!
    "crop_mode": True,     # 🔥 Автоматическая обрезка значимых областей!
}
```

**Как это работает:**
1. Модель находит значимые области на изображении (диаграмму)
2. Обрезает пустые поля
3. Сканирует по сегментам (640px окна)
4. Собирает результаты вместе

---

### 4. Структурированное извлечение связей - МОДЕЛЬ УМЕЕТ! ✅

**Цитата:**
> "В режиме Parse Figure модель не просто читает текст, но и пытается интерпретировать схему как граф: выявлять, какие надписи принадлежат каким фигурам, какие фигуры соединены линиями и под какими подписями. По сути, происходит распознавание структуры диаграммы."

**Пример ожидаемого вывода:**
```
Блок Start (стартовый круг) связанный стрелкой Yes с блоком Task 1
Блок Task 1 стрелкой без подписи ведёт к ромбу Decision (решение)
Ромб Decision имеет два выхода:
  - стрелка "Approve" → Task 2
  - стрелка "Reject" → End Event
```

**ЧТО ЭТО ЗНАЧИТ:**
- ✅ Модель ОБУЧЕНА извлекать граф из диаграмм!
- ✅ Не нужен Smart Merge - модель сделает всё сама!
- ✅ Можно получить готовую структуру для BPMN IR

---

### 5. Мультиязычный вывод - ПРОСТО ПОПРОСИТЬ НА РУССКОМ! 🇷🇺

**Цитата:**
> "Чтобы получить описание элементов диаграммы на русском, можно в тексте запроса (промпте) задавать инструкцию на русском языке. Например, добавить фразу 'на русском' или изначально формулировать запрос по-русски – DeepSeek-OCR была обучена на многоязычных данных и может выдавать результаты OCR в том числе по-русски."

**ПРАВИЛЬНЫЙ ПРОМПТ:**
```
<image>
Разбери данную BPMN диаграмму:
1. Перечисли все элементы (процессы, события, шлюзы)
2. Укажи их типы (задача, решение, начало, конец)
3. Опиши связи между ними (какие стрелки куда ведут)
4. Ответ дай структурированно на русском языке.
```

---

### 6. Grounded OCR - КООРДИНАТЫ + СТРУКТУРА! 📍

**Цитата:**
> "Grounded OCR (с привязкой к макету) – распознавание с информацией о расположении: помимо текста возвращаются координаты блоков (bounding boxes) и структура страницы. Этот режим полезен для форм, таблиц, схем – где важно знать, где на изображении находится каждый фрагмент текста."

**КАК ИСПОЛЬЗОВАТЬ:**
```python
# Для получения координат + текста:
prompt = "<image>\n<|grounding|>Extract all text from BPMN elements with bounding boxes."
```

**Токен `<|grounding|>`** - переключает в режим с координатами!

---

## 🎯 ПЕРЕОЦЕНКА ВЫВОДОВ ЭКСПЕРИМЕНТА A

### ЧТО МЫ ДУМАЛИ:

❌ "Локальная модель DeepSeek-OCR НЕ умеет извлекать текст из визуальных элементов BPMN"

### ПРАВДА:

✅ **Модель УМЕЕТ, но мы использовали неправильные параметры и режимы!**

**Что мы НЕ делали:**
1. ❌ НЕ использовали Large mode (1280x1280)
2. ❌ НЕ активировали crop_mode
3. ❌ НЕ использовали Gundam режим
4. ❌ НЕ просили явно извлечь структуру графа
5. ❌ НЕ использовали `<|grounding|>` токен для координат
6. ❌ НЕ формулировали промпт структурированно

---

## 📋 НОВЫЙ ПЛАН ТЕСТИРОВАНИЯ (Эксперимент B)

### Тест 1: Parse Figure + Large Mode + Русский вывод

```python
{
    "name": "Parse Figure (Large, Русский)",
    "prompt_type": "parse_figure",
    "custom_prompt": "Разбери BPMN диаграмму: перечисли элементы и связи. Ответ на русском.",
    "base_size": 1280,
    "image_size": 1280,
    "crop_mode": False
}
```

**Ожидание:** Структурированное описание графа на русском

---

### Тест 2: Parse Figure + Gundam Mode

```python
{
    "name": "Parse Figure (Gundam)",
    "prompt_type": "parse_figure",
    "custom_prompt": "Parse BPMN: list elements, types, and connections.",
    "base_size": 1024,
    "image_size": 640,    # 🔥 Фрагментация!
    "crop_mode": True      # 🔥 Автообрезка!
}
```

**Ожидание:** Детальное извлечение с фокусом на диаграмме

---

### Тест 3: Grounded OCR + Large Mode

```python
{
    "name": "Grounded OCR (Large)",
    "prompt_type": "default",  # или кастомный с <|grounding|>
    "custom_prompt": "<image>\n<|grounding|>Extract BPMN elements with coordinates. Output in Russian.",
    "base_size": 1280,
    "image_size": 1280,
    "crop_mode": True
}
```

**Ожидание:** Координаты + текст BPMN элементов

---

### Тест 4: Комбинированный (Структура + Координаты)

**Стратегия:**
1. **Запрос 1:** Parse Figure (Large) → структура графа
2. **Запрос 2:** Grounded OCR (Large) → координаты элементов
3. **Merge:** Сопоставить по названиям элементов

**Ожидание:** ПОЛНАЯ информация - текст, связи, координаты, типы

---

## 🔧 ИЗМЕНЕНИЯ В КОД

### 1. Поддержка новых параметров в API

Наш `app.py` уже поддерживает:
- ✅ `base_size`
- ✅ `image_size`
- ✅ `crop_mode`
- ✅ `custom_prompt`

**НО нужно добавить:**
- [ ] Токен `<|grounding|>` в промпт при необходимости
- [ ] Параметр `temperature=0` для детерминированности
- [ ] Параметр `max_tokens=8192` для длинных выводов

### 2. Новая утилита для тестирования

Создать `test_parse_figure_advanced.py`:
```python
# Тестирование всех комбинаций:
# - Parse Figure (base/large/gundam)
# - Grounded OCR (large)
# - Русские инструкции
# - Структурированный вывод
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Если всё сработает:

**✅ Parse Figure (Large) → структура:**
```markdown
# BPMN Диаграмма: Требования к моделированию

## Элементы процесса:

1. **Процесс 1** (Задача)
   - Тип: Task
   - Выходные связи: → Процесс 2

2. **Процесс 2** (Задача)
   - Тип: Task
   - Входные связи: Процесс 1 →
   - Выходные связи: → Решение

3. **Решение** (Шлюз)
   - Тип: Gateway
   - Входные связи: Процесс 2 →
   - Выходные связи:
     - [Да] → Процесс 3
     - [Нет] → Событие 1

4. **Процесс 3** (Задача)
   - Тип: Task
   - Входные связи: Решение [Да] →
   - Выходные связи: → Событие 2

5. **Событие 1** (Начальное событие)
   - Тип: Start Event
   - Выходные связи: → Процесс 1

6. **Событие 2** (Конечное событие)
   - Тип: End Event
   - Входные связи: Процесс 3 →
```

**✅ Grounded OCR (Large) → координаты:**
```
<|ref|>task<|/ref|><|det|>[[350, 200, 450, 250]]<|/det|>Процесс 1
<|ref|>task<|/ref|><|det|>[[550, 200, 650, 250]]<|/det|>Процесс 2
<|ref|>gateway<|/ref|><|det|>[[700, 190, 750, 260]]<|/det|>Решение
<|ref|>task<|/ref|><|det|>[[850, 200, 950, 250]]<|/det|>Процесс 3
<|ref|>event<|/ref|><|det|>[[270, 210, 320, 240]]<|/det|>Событие 1
<|ref|>event<|/ref|><|det|>[[1050, 210, 1100, 240]]<|/det|>Событие 2
```

**➡️ BPMN IR (готово для XML!) →**
```json
{
  "elements": [
    {
      "id": "event_1",
      "type": "startEvent",
      "name": "Событие 1",
      "bbox": [270, 210, 320, 240],
      "outgoing": ["flow_1"]
    },
    {
      "id": "task_1",
      "type": "task",
      "name": "Процесс 1",
      "bbox": [350, 200, 450, 250],
      "incoming": ["flow_1"],
      "outgoing": ["flow_2"]
    },
    // ... остальные элементы
  ],
  "flows": [
    {"id": "flow_1", "source": "event_1", "target": "task_1"},
    {"id": "flow_2", "source": "task_1", "target": "task_2"},
    // ... остальные связи
  ]
}
```

---

## 🚀 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### Шаг 1: Обновить код (15 мин)
- [ ] Добавить поддержку `<|grounding|>` токена
- [ ] Создать промпты для Parse Figure (структурированные)
- [ ] Добавить параметры `temperature`, `max_tokens`

### Шаг 2: Создать тестовую утилиту (30 мин)
- [ ] `test_parse_figure_advanced.py`
- [ ] Тестирование 6 режимов:
  - Parse Figure (base)
  - Parse Figure (large)
  - Parse Figure (gundam)
  - Grounded OCR (large)
  - Parse Figure + русский
  - Grounded + русский

### Шаг 3: Запустить тесты (30 мин)
- [ ] Протестировать на `page_54_fresh_300dpi.png`
- [ ] Сравнить результаты с baseline
- [ ] Выбрать лучший режим

### Шаг 4: Документировать (30 мин)
- [ ] Обновить `Experiment_B_Results.md`
- [ ] Обновить `Changelog.md`
- [ ] Обновить `Project_Status`

---

## 💡 КЛЮЧЕВЫЕ ИНСАЙТЫ

### 1. Parse Figure - это ВСЁ, что нам нужно!

Если модель действительно умеет извлекать граф (как обещает документация), то:
- ❌ НЕ нужен Smart Merge
- ❌ НЕ нужен Вариант B (комбинированный)
- ✅ Parse Figure (Large) → прямо в BPMN IR!

### 2. Crop Mode - критичен для BPMN

BPMN диаграммы часто имеют:
- Много пустого пространства вокруг
- Мелкий текст внутри элементов
- Сложную структуру

`crop_mode=True` → фокусируется на диаграмме → лучшее качество

### 3. Размер имеет значение

- 1024x1024 (256 tokens) - для простых схем
- 1280x1280 (400 tokens) - для BPMN с мелкими деталями
- Gundam (1024/640) - для очень сложных или больших диаграмм

---

## 🔮 ПРОГНОЗ

### Если тесты покажут успех Parse Figure (Large):

**Новая архитектура будет простой:**

```
PDF → Pages → BPMN Detection →
  ↓
For each BPMN page:
  ↓
Parse Figure (Large mode, crop=True, Russian) →
  ↓
Structured Description (граф на русском) →
  ↓
BPMN IR (JSON) →
  ↓
BPMN XML (Camunda compatible) →
  ↓
DONE! ✅
```

**Время реализации:** 4-6 часов  
**Ожидаемая точность:** 85-95%  
**Сложность:** Низкая (без fuzzy matching, без комбинирования)

---

## 📚 ИСТОЧНИКИ

**Исследование предоставлено пользователем:**
- OCR_INTEGRATION.md (GitHub)
- DeepSeek-OCR Paper (ArXiv)
- DeepSeek-OCR HuggingFace docs
- DeepSeek-VL2 docs

**Ключевые цитаты сохранены в:** `docs/Research_Quotes.md`

---

**Статус:** 🔥 **READY TO TEST!**  
**Следующий шаг:** Реализовать тесты и проверить, работает ли Parse Figure (Large) как обещано!

