# 🌍 DeepSeek-OCR: Анализ многоязычной поддержки

**Дата:** 31.10.2025  
**Источник:** Скриншот из https://github.com/deepseek-ai/DeepSeek-OCR/blob/main/DeepSeek_OCR_paper.pdf

---

## 📋 ЧТО МЫ УЗНАЛИ ИЗ ДОКУМЕНТАЦИИ

### Цитата из статьи (секция 4.3.2. Multilingual recognition):

> "PDF data on the Internet contains not only Chinese and English, but also a large amount of 
> multilingual data, which is also crucial when training LLMs. For PDF documents, **DeepSeek-OCR 
> can handle nearly 100 languages.** Like Chinese and English documents, **multilingual data 
> also supports both layout and non-layout OCR formats**. The visualization results are shown in 
> Figure 11, where we select Arabic and Sinhala languages to demonstrate results."

---

## 🎯 КЛЮЧЕВЫЕ ВЫВОДЫ:

### ✅ DeepSeek-OCR ПОДДЕРЖИВАЕТ:

1. **~100 языков** (включая русский, кириллицу)
2. **Layout OCR** - с сохранением структуры документа
3. **Non-layout OCR** - простое извлечение текста
4. **Многоязычные документы** - смешанный текст (русский + английский)

### ⚠️ НО: Проблема с кириллицей в наших экспериментах

**Что мы видели:**
```
ocr_simple выдавал:
  npoecc1  → должно быть "Процесс 1"
  C6bITHe1 → должно быть "Событие 1"
```

**Возможные причины:**

#### Причина 1: Неправильный режим OCR
Модель использовала **Layout OCR** вместо **Non-layout OCR**
- Layout OCR пытается сохранить координаты → искажает кириллицу
- Non-layout OCR просто извлекает текст → может работать лучше

#### Причина 2: Промпт не указывает язык
```python
# Мы использовали:
prompt = "<image>\n<|grounding|>..."  # нет указания языка

# Возможно нужно:
prompt = "<image>\n<|grounding|>Russian text. Convert to markdown..."
# или
prompt = "<image>\nLanguage: Russian. Extract all text..."
```

#### Причина 3: Системный промпт `ocr_simple` оптимизирован для English/Chinese
- Внутренний промпт `ocr_simple` может быть заточен под латиницу/иероглифы
- Кириллица обрабатывается как "похожая на латиницу" → транслитерация

---

## 🧪 ЭКСПЕРИМЕНТЫ ДЛЯ ПРОВЕРКИ ГИПОТЕЗ

### Эксперимент 1: Явное указание языка в промпте ⭐⭐⭐⭐

**Тест:**
```python
prompts = [
    # Базовый (для сравнения)
    "<image>\n<|grounding|>",
    
    # С явным указанием языка
    "<image>\n<|grounding|>Language: Russian. ",
    "<image>\n<|grounding|>Язык: Русский. ",
    "<image>\nRussian OCR. Extract all text with coordinates.",
    
    # Более детальные инструкции
    "<image>\n<|grounding|>This is a Russian BPMN diagram. Extract all text preserving Cyrillic characters.",
]
```

**Ожидаемый результат:**
- Если проблема в отсутствии указания языка → кириллица распознается правильно
- Если проблема глубже → искажения остаются

**Рекомендация:** ⭐⭐⭐⭐⭐ **НАЧАТЬ С ЭТОГО**

---

### Эксперимент 2: Non-layout режим ⭐⭐⭐

**Тест:**
```python
# Layout mode (текущий)
prompt = "<image>\n<|grounding|>..."  # с координатами

# Non-layout mode
prompt = "<image>\nFree OCR."  # без координат
prompt = "<image>\nExtract plain text without layout."
```

**Ожидаемый результат:**
- Non-layout может работать лучше с кириллицей
- Но потеряем координаты элементов

**Рекомендация:** ⭐⭐⭐ Если Эксперимент 1 не поможет

---

### Эксперимент 3: Разные токенизаторы ⭐⭐

**Проблема:**
Возможно токенизатор не правильно обрабатывает кириллицу

**Тест:**
```python
# Проверить, что токенизатор поддерживает кириллицу
tokenizer = AutoTokenizer.from_pretrained('deepseek-ai/DeepSeek-OCR')
test_text = "Процесс 1, Событие 1"
tokens = tokenizer.encode(test_text)
decoded = tokenizer.decode(tokens)
print(f"Original: {test_text}")
print(f"Decoded:  {decoded}")
# Если decoded != test_text → проблема в токенизаторе
```

**Рекомендация:** ⭐⭐ Для глубокой диагностики

---

### Эксперимент 4: Прямое использование LLM декодера ⭐

**Идея:**
DeepSeek-OCR = Vision Encoder + LLM Decoder

Может быть проблема в Vision Encoder (искажает кириллицу), а LLM Decoder нормальный?

**Тест:**
1. Извлечь vision embeddings из изображения
2. Отправить напрямую в LLM с промптом "Decode Russian text"
3. Проверить, сохраняется ли кириллица

**Рекомендация:** ⭐ Сложно, только если все остальное не помогло

---

## 💡 ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ

### Вариант A: Улучшить промпты (БЫСТРО) ⭐⭐⭐⭐⭐

**Что делать СЕЙЧАС:**

```python
# Добавить в pdf_to_context/ocr_service/prompts.py

@staticmethod
def get_russian_layout_ocr_prompt() -> str:
    """Промпт для русского текста с координатами"""
    return (
        "<image>\n<|grounding|>"
        "Language: Russian. "
        "Extract all text with coordinates. "
        "Preserve Cyrillic characters exactly as they appear."
    )

@staticmethod
def get_russian_bpmn_prompt() -> str:
    """Промпт для русских BPMN диаграмм"""
    return (
        "<image>\n<|grounding|>"
        "This is a BPMN diagram with Russian text. "
        "Extract all text from diagram elements (processes, events, gateways). "
        "Language: Russian (Cyrillic). "
        "Provide coordinates for each element."
    )
```

**Тестирование:**
1. Запустить с новыми промптами на `page_54_bpmn.png`
2. Проверить, исчезла ли проблема с транслитерацией
3. Если да → использовать этот промпт вместо `ocr_simple`

**Время:** 1-2 часа  
**Вероятность успеха:** 70-80%

---

### Вариант B: Комбинированный подход (ОПТИМАЛЬНО) ⭐⭐⭐⭐⭐

**Если Вариант A не решит проблему:**

```
1. parse_figure/describe → Правильные русские названия
   "Процесс 1", "Событие 1" ✅

2. default (не ocr_simple!) → Координаты
   Промпт default работает лучше с кириллицей
   
3. Smart Merge → НО упрощенный (без 7 компонентов)
   - Простое сопоставление по позициям
   - Без fuzzy matching
   - Без нормализации транслита
```

**Преимущества:**
- Используем правильные названия из describe
- Координаты из default (который работает лучше)
- Простая логика (3-4 компонента, не 7)

**Время:** 3-4 часа  
**Точность:** 80-90%

---

### Вариант C: Fine-tuning (ДОЛГОСРОЧНО) ⭐⭐⭐⭐

**Если ничего не помогает:**

1. Собрать датасет русских BPMN (100-200 примеров)
2. Разметить: элементы + координаты + правильные названия
3. Fine-tune DeepSeek-OCR на этом датасете
4. Модель научится правильно работать с русскими диаграммами

**Время:** 20-30 часов  
**Точность:** 90-95%

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Шаг 1 (СЕЙЧАС): Тест с явным указанием языка

```bash
# Создать новые промпты с "Language: Russian"
# Протестировать на page_54_bpmn.png
# Сравнить результаты
```

**Если успех → решение найдено! ✅**

### Шаг 2 (если Шаг 1 не помог): Non-layout режим

```bash
# Попробовать "Free OCR" вместо grounding
# Потеряем координаты, но получим правильный текст
# Можем компенсировать через describe
```

### Шаг 3 (если Шаги 1-2 не помогли): Комбинированный подход

```bash
# describe → правильные названия
# default → координаты  
# Простой merge по позициям
```

### Шаг 4 (долгосрочно): Fine-tuning

```bash
# Только если обрабатываем >1000 документов
# И нужна максимальная точность
```

---

## 📊 СРАВНЕНИЕ ПОДХОДОВ

| Подход | Время | Сложность | Точность | Зависимости |
|--------|-------|-----------|----------|-------------|
| **Улучшенные промпты** | 1-2ч | Низкая | 70-80% | Нет |
| **Комбинированный** | 3-4ч | Средняя | 80-90% | Нет |
| **Fine-tuning** | 20-30ч | Высокая | 90-95% | Датасет + GPU |
| **GPT-4V API** | 2-4ч | Низкая | 95-98% | API ключ ($) |

---

## 🚀 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

1. **Добавить новые промпты** с явным указанием русского языка
2. **Протестировать** на `page_54_bpmn.png`
3. **Сравнить** с текущими результатами
4. **Принять решение** на основе результатов

**Начинаем с Эксперимента 1?** 

Это займет ~1 час и может ПОЛНОСТЬЮ решить проблему! 🎯

